# Footer

The live footer (`footer#footer.footer`) is rendered client-side (JS/widgets) and is empty in the static HTML, so no copy was captured. The footer/side-panel demo content on the old site (e.g. a New York mailing address) is WGL theme placeholder, not real.
